# Backend API base URL for production deployment (e.g., https://your-backend-api.onrender.com/api)
# In development or when using a proxy, this can be left blank or omitted to default to /api
REACT_APP_API_URL=https://your-mindvault-backend.onrender.com/api
